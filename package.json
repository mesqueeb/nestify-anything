{
  "name": "nestify-anything",
  "version": "4.0.1",
  "description": "Recreates an object from any `nested.props`. A simple and small integration.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit && eslint ./src",
    "build": "del-cli dist && tsc",
    "release": "pnpm run lint && pnpm run build && pnpm test && bumpp --no-push --all && pnpm publish --no-git-checks && npx jsr publish ${JSR_TOKEN:+--token $JSR_TOKEN} && git push --follow-tags && gh release create $(git describe --tags --abbrev=0) --generate-notes --notes \"$(git log --pretty='- %s  %h' $(git describe --tags --abbrev=0 HEAD~1 2>/dev/null || git rev-list --max-parents=0 HEAD)..HEAD~1)\""
  },
  "dependencies": {
    "merge-anything": "^6.0.6"
  },
  "devDependencies": {
    "@cycraft/eslint": "^0.4.7",
    "@cycraft/tsconfig": "^0.1.2",
    "bumpp": "^12.3.0",
    "del-cli": "^7.0.0",
    "eslint": "^9.20.1",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "nestify",
    "nest-objects",
    "nestify-objects",
    "nested",
    "paths",
    "nest-paths"
  ],
  "author": "Luca Ban - Mesqueeb (https://cycraft.co)",
  "funding": "https://github.com/sponsors/mesqueeb",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mesqueeb/nestify-anything.git"
  },
  "homepage": "https://github.com/mesqueeb/nestify-anything#readme",
  "bugs": "https://github.com/mesqueeb/nestify-anything/issues",
  "packageManager": "pnpm@11.27.0"
}
